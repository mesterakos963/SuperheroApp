<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/design_40dp"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/markerViewRoot"
    android:background="@drawable/marker_background">

    <TextView
        android:id="@+id/markerText"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:text="100"
        android:padding="@dimen/design_4dp"
        android:gravity="center"
        android:textAppearance="@style/marker_text_font" />

</FrameLayout>